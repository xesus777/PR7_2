<Window x:Class="PR7_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PR7_2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="ROT13 Шифрование/Дешифрование" 
                   FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Поле ввода -->
        <TextBlock Grid.Row="1" Text="Входной текст:" Margin="0,5"/>
        <TextBox Grid.Row="2" x:Name="InputTextBox" 
                 Height="100" TextWrapping="Wrap" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
            <Button x:Name="EncryptButton" Content="Зашифровать" 
                    Width="120" Height="30" Margin="5"
                    Click="EncryptButton_Click"/>
            <Button x:Name="DecryptButton" Content="Расшифровать" 
                    Width="120" Height="30" Margin="5"
                    Click="DecryptButton_Click"/>
            <Button x:Name="ClearButton" Content="Очистить" 
                    Width="120" Height="30" Margin="5"
                    Click="ClearButton_Click"/>
        </StackPanel>

        <!-- Поле вывода -->
        <TextBlock Grid.Row="4" Text="Выходной текст:" Margin="0,5"/>
        <TextBox Grid.Row="5" x:Name="OutputTextBox" 
                 Height="100" TextWrapping="Wrap" 
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 Background="#F5F5F5"
                 Margin="0,0,0,10"/>

        <!-- Статус -->
        <TextBlock Grid.Row="6" x:Name="StatusTextBlock" 
                   Text="Готов" 
                   Foreground="Gray"
                   Margin="0,5"/>
    </Grid>
</Window>
